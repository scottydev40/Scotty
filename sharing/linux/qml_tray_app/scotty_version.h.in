#ifndef SCOTTY_LINUX_QML_TRAY_APP_SCOTTY_VERSION_H_
#define SCOTTY_LINUX_QML_TRAY_APP_SCOTTY_VERSION_H_

// Generated by CMake from scotty_version.h.in. Do not edit the generated copy.
//
// SCOTTY_VERSION_TAG is the release tag of this build, resolved at configure
// time via `git describe --tags` (falls back to the CMake project version when
// git is unavailable, e.g. a source tarball). The update checker parses it with
// scotty::ParseTag and compares it against GitHub release tags.
#define SCOTTY_VERSION_TAG "@SCOTTY_VERSION_TAG@"

// The GitHub repository the update checker queries for releases.
#define SCOTTY_UPDATE_REPO "@SCOTTY_UPDATE_REPO@"

// The release asset filename this build self-updates from (AppImage path).
#define SCOTTY_APPIMAGE_ASSET "@SCOTTY_APPIMAGE_ASSET@"

#endif  // SCOTTY_LINUX_QML_TRAY_APP_SCOTTY_VERSION_H_
