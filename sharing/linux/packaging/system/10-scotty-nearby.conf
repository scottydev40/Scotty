# Scotty requires BlueZ experimental D-Bus interfaces for Nearby transports.
# Disabling MCP prevents BlueZ's experimental media-control plugin from
# initiating an unrelated pairing flow during a transfer.
[Service]
ExecStart=
ExecStart=/usr/libexec/bluetooth/bluetoothd --experimental --noplugin=mcp
