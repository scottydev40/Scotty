# fork-local: empty gloop stub package. Exists only so labels referenced in
# unselected select() arms / non-Linux platform rules resolve at loading time.
# NOT real gloop — never vendor or build gloop in this fork.
load("@rules_cc//cc:defs.bzl", "cc_library")

package(default_visibility = ["//visibility:public"])

cc_library(name = "thread")
