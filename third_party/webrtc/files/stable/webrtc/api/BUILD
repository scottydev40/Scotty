# fork-local: empty webrtc stub package. Exists only so labels referenced in
# unselected select() arms / non-Linux platform rules resolve at loading time.
# NOT real webrtc — never vendor or build webrtc in this fork.
load("@rules_cc//cc:defs.bzl", "cc_library")

package(default_visibility = ["//visibility:public"])

cc_library(name = "create_modular_peer_connection_factory")

cc_library(name = "create_peerconnection_factory")

cc_library(name = "peer_connection_interface")

cc_library(name = "scoped_refptr")
